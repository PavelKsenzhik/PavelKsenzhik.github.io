import { connect } from 'react-redux';

function Branche({  }) {

    return (
        <div>
            {}
        </div>
    )
}

const mapStateToProps = (state) => ({
    
})

export default connect(mapStateToProps)(Branche);